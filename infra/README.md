# infra — deployment & operations

Containers, deployment, and CI for the two runtime processes (grounding engine + backend) and the
client's release pipeline. Populated once the layer topology and API contracts are locked in `docs/`.
